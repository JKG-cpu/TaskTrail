Grid {
    grid-size: 2;
    grid-rows: 1fr 1fr;
    grid-columns: 1fr 1fr;
}

Button {
    background: transparent;
    border: solid $panel;
    color: $text;
    width: 100%;
    text-style: none;
}

Tabs {
    padding-top: 1;
    height: 3;
    background: transparent;
}

LoginPage {
    align: center middle;
    width: 100%;
    height: 100%;
}

.title {
    border: solid $panel;
    text-align: center;
    width: 100%;
}
